#include <iostream>
using namespace std;
int main()
{
    float radius;
    cout << "Enter radius of the circle: ";
    cin >> radius;
    cout << "Areof the circle of radius: " << radius << " is:" << 3.14 * radius * radius;
    return 0;
}