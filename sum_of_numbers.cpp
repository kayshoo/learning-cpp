#include <iostream>
using namespace std;
int main()
{
    float x, y;
    cout << "Enter two numbers: ";
    cin >> x >> y;
    cout << "the sum of " << x << " and " << y << " is " << x + y;
    return 0;
}